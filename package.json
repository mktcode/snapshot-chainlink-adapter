{
  "name": "@mktcode/snapshot-chainlink-adapter",
  "version": "0.0.1",
  "description": "A chainlink adapter to bring Snapshot proposal results on chain.",
  "main": "adapter.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node server"
  },
  "author": "mktcode",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  }
}
